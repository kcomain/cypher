hero: Easy to use PyMongo Wrapper
# Mongo
This page doucuments the usage and examples utilizing the `Mongo` class

## Synopsis
Using the `Mongo` class is very easy. The first step is import the module.

Work in progress.